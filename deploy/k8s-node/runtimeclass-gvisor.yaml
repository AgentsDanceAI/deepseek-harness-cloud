# k3s kubectl apply -f runtimeclass-gvisor.yaml —— 前提: containerd 里已有 runsc 运行时
# (见 containerd-config-v3.toml.tmpl)。后端按 K8S_GVISOR_PRODUCTS 给 Pod 加 runtimeClassName。
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
