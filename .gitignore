__pycache__/
*.pyc
.venv*/
server/data/
desktop/build/
node_modules/
.pytest_cache/
*.egg-info/

# secrets — never commit
deploy/**/.env
.env
!deploy/**/.env.template
!deploy/**/.env.example

# electron-builder downloads ~150MB of Electron/NSIS/winCodeSign per build;
# build-win.sh keeps it here so rebuilds are fast, but it is a cache, not source.
desktop/.cache-electron/
