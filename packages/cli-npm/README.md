# `@agentsdanceai/dsh-cloud`

Zero-runtime-dependency installer and lifecycle CLI for DSH Cloud Community
Edition. Run `dsh-cloud --help`; trial deployments bind to `127.0.0.1` by
default. This source package is staged by `scripts/release/build-packages.mjs`,
which embeds the canonical Compose templates, configuration, and release
manifest before publication.
