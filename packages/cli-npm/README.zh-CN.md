# `@agentsdanceai/dsh-cloud`

[English](README.md) | 简体中文

DSH Cloud Community Edition 的零运行时依赖安装和生命周期 CLI。运行
`dsh-cloud --help` 查看命令；试用部署默认绑定 `127.0.0.1`。
`scripts/release/build-packages.mjs` 会在发布前嵌入标准 Compose 模板、配置和
发行清单。
